#import sqlalchemy

#engine = sqlalchemy.create_engine("sqlite:///european_database.sqlite")

#conn = engine.connect()

#metadata = sqlalchemy.MetaData()
#division = sqlalchemy.Table('divisions', metadata, autoload=True, autoload_with=engine)